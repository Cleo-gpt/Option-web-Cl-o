class Planete {
  constructor(nom, distanceUa, satellites, type) {
    this.nom = nom;
    this.distanceUa = distanceUa;
    this.satellites = satellites;
    this.type = type;
  }

  estGazeuse() {
    /* à vous : une ligne */
  }

  plusLoinQue(autre) {
    /* à vous : une ligne */
  }
}

/* Étape 2 : fabriquez seulement Terre ici. */

/* Étape 5 : remplacez les objets de test par le catalogue et son affichage. */
